<div class="card card-position">
  <div class="card-body">
    <div class="container conteiner-card">
      <div class="row">
        <div class="col-2">
          <%= image_tag position.company.logo, alt: "Logo marca da empresa #{position.company.name}", class: 'card-img-top'%>
        </div>
        <div class="col-10">
          <h4><%= position.name %></h4>
          <h5><%= "#{I18n.t('activerecord.attributes.position.careers')[position.career.to_sym]} - #{position.city} - #{position.state} - #{I18n.t('activerecord.attributes.position.contracts')[position.contract.to_sym]}" %></h5>
          <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Totam officia porro ab, nihil quis earum ratione repudiandae, ipsam neque quidem iusto commodi ipsum dolor cumque repellat fugiat corporis voluptatibus eaque.</p>
          <div class="row">
            <div class="col-sm mt-3">
              <%= link_to 'Ver mais', public_position_path(position.slug), class: 'btn btn-danger' %>
            </div>
            <div class="col-sm text-right mt-3 group-social-network">
              <%= render partial: 'shared/social_network', locals: { position: position } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
